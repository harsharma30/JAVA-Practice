/*
 * Arrays:
 * Store similar value
 * works with loop
 * allocate dynamic memory
 * Have method/ property(arr.length())
 * Retrieval would be faster
 * Size of array is always fixed
 * Array unit can be modified but element can never be deleted..
 * 
 * 1.Declaration
 * 2.Creation
 * 3.Initialisation
 * 4.Access
 * 
 */